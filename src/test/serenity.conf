webdriver {
    driver = chrome
    timeouts {
        implicitlywait = 60000
        fluentwait = 10000
      }
}

serenity {
    project.name = "WordCounter Automation"
    take.screenshots = AFTER_EACH_STEP
}
